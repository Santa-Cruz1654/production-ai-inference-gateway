[project]
name = "production-ai-inference-gateway"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "groq>=1.7.0",
    "pydantic>=2.13.5",
]

[dependency-groups]
dev = [
    "pytest-asyncio>=1.4.0",
]
